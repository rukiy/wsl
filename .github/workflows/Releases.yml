name: Releases
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - name: checkout 
      uses: actions/checkout@v2
    - name: build
      run: cd alpine && sh build